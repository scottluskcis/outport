{
  "name": "@scottluskcis/export-toolkit",
  "version": "1.0.0",
  "description": "Tool for exporting data to a format that can be used for reporting such as CSV, JSON, etc.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "sideEffects": false,
  "publishConfig": {
    "access": "public"
  },
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc --noEmit",
    "ci": "pnpm run typecheck && pnpm run lint && pnpm run format:check && pnpm run build && pnpm run test",
    "prepare": "husky",
    "prepublishOnly": "pnpm run ci",
    "release:check": "pnpm run ci && echo '' && echo '✅ Ready for release! Next steps:' && echo '1. Update CHANGELOG.md' && echo '2. Run: npm version [patch|minor|major]' && echo '3. Run: git push && git push --tags' && echo '4. Create GitHub release'"
  },
  "keywords": [
    "export",
    "csv",
    "json",
    "data-export",
    "reporting",
    "typescript",
    "streaming",
    "builder-pattern",
    "file-writer"
  ],
  "author": "scottluskcis",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/scottluskcis/export-toolkit.git"
  },
  "bugs": {
    "url": "https://github.com/scottluskcis/export-toolkit/issues"
  },
  "homepage": "https://github.com/scottluskcis/export-toolkit#readme",
  "devDependencies": {
    "@types/node": "^24.8.1",
    "@typescript-eslint/eslint-plugin": "^8.12.2",
    "@typescript-eslint/parser": "^8.12.2",
    "@vitest/coverage-v8": "^3.2.4",
    "eslint": "^9.38.0",
    "eslint-config-prettier": "^10.1.8",
    "husky": "^9.1.6",
    "lint-staged": "^16.2.4",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "vitest": "^3.2.4"
  },
  "lint-staged": {
    "*.ts": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md,yml,yaml}": [
      "prettier --write"
    ]
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@9.12.3"
}
